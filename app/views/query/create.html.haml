- if defined?(@greper) && @greper.parsed_data.present?
  %h4
    Results for:
    = @greper.product_id
  - @greper.parsed_data.each do |page, html|
    %p
    %p= raw html
- else
  %h4 There is now reviews for this query